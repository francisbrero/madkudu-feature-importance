# madkudu-feature-importance

Use Gini Impurity to calculate feature importance in MadKudu tree models

## Run locally

Start a venv

```bash
python3 -m venv venv
source venv/bin/activate
```

Install dependencies

```bash
pip install -r requirements.txt
```

Run the script

```bash
streamlit run app.py
```